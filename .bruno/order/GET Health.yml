info:
  name: GET Health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:30077/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
