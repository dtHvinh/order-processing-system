info:
  name: GET Get all order
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:30077/api/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
