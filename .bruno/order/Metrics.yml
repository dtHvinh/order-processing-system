info:
  name: Metrics
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:30081/api/prometheus/metrics
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
