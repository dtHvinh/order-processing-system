info:
  name: GET Health
  type: http
  seq: 6

http:
  method: GET
  url: "{{uo}}inventories/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
