info:
  name: GET All Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{uo}}products?limit=5&offset=0"
  params:
    - name: limit
      value: "5"
      type: query
    - name: offset
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
