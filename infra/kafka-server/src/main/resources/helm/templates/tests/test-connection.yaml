apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-server.fullname" . }}-test-connection"
  labels:
    {{- include "kafka-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kafka-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
